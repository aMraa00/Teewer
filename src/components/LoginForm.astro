<section id="login" class="py-46 bg-gray-150">
    <div class="container mx-auto max-w-md bg-white p-8 rounded-lg shadow-lg">
      <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Нэвтрэх</h2>
      <form id="loginForm">
        <div class="mb-4">
          <label for="username" class="block text-gray-600">Хэрэглэгчийн нэр</label>
          <input type="text" id="username" name="username" class="w-full p-3 border rounded-lg" required />
        </div>
        <div class="mb-4">
          <label for="password" class="block text-gray-600">Нууц үг</label>
          <input type="password" id="password" name="password" class="w-full p-3 border rounded-lg" required />
        </div>
        <div class="flex justify-between items-center">
          <button type="submit" class="w-full bg-blue-700 text-white p-3 rounded-lg hover:bg-red-400 font-bold">Нэвтрэх</button>
          </div>
          <div>
      </form>
    </div>
  </section>
  
  <!-- Client-side script for login functionality -->
  <!-- <script client:load>
    document.addEventListener('DOMContentLoaded', function () {
      const form = document.getElementById('loginForm');
      if (form) {
        form.addEventListener('submit', function (event) {
          event.preventDefault();
          
          const username = document.getElementById('username').value;
          const password = document.getElementById('password').value;
  
          console.log('Нэвтрэх мэдээлэл:', { username, password });
  
          // Энд хэрэглэгчийн мэдээллийг сервер рүү илгээх код оруулах болно.
        });
      }
    });
  </script>
   -->